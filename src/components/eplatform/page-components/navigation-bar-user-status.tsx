import React from 'react';
// import { ApplicationStateType } from '../../store/types';
// import UserMenu from '../features/auth/components/user-menu';
// import AuthDialog from '../features/auth/sign-in/auth-dialog';

function NavigationBarUserStatus() {
  /*  const { email } = useSelector(
    (state: ApplicationStateType) => state.signInReducer,
  );
  return <>{email ? <UserMenu /> : <AuthDialog />}</>;*/
  return <h1>UserMenu and AuthDialog</h1>;
}

export default NavigationBarUserStatus;
